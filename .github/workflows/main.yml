name: CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Download pharo
      run: |
        Invoke-WebRequest -Uri https://dl.feenk.com/gt/GToolkitWin64-v0.7.174.zip -OutFile GToolkitWin64-v0.7.1741.zip -UseBasicParsing
        ls
        Expand-Archive GToolkitWin64-v0.7.174.zip -DestinationPath .
         .\GToolkitWin64-v0.7.174\PharoConsole.exe .\GToolkitWin64-v0.7.174\GToolkit-64-v0.7.174-2019091809544664.image --no-quit
      shell: powershell


      