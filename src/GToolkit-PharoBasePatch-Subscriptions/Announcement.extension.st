Extension { #name : #Announcement }

{ #category : #'*GToolkit-PharoBasePatch-Subscriptions' }
Announcement >> gtDoneAnnouncementIn: anAnnouncer [
	"If users need to be sure that all necessary work is finished after the original announcement,
	a new announcement (called `AnnouncementDone`) can be announced using this method."
]
